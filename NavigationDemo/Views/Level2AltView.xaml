<UserControl x:Class="NavigationDemo.Views.Level2AltView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:nav="clr-namespace:NavigationLib.FrameworksAndDrivers;assembly=NavigationLib"
             xmlns:local="clr-namespace:NavigationDemo.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFCCBC" Padding="10" Margin="0,0,0,5">
            <StackPanel>
                <TextBlock Text="Level 2 Layer (from L1-B)" FontSize="13" FontWeight="Bold"/>
                <TextBlock Text="{Binding CurrentSegment}" FontSize="9" Foreground="DarkRed" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- TabControl for Level3 -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="L2-C">
                <local:Level3View x:Name="Level3C" nav:Region.Name="Level3C"/>
            </TabItem>
            <TabItem Header="L2-D">
                <local:Level3View x:Name="Level3D" nav:Region.Name="Level3D"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
