<UserControl x:Class="NavigationDemo.Views.Level3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#F1F8E9" Padding="20">
        <StackPanel>
            <TextBlock Text="✓ Level 3: Final Destination" 
                      FontSize="16" FontWeight="Bold" 
                      Foreground="Green" Margin="0,0,0,15"/>
            
            <Border Background="White" BorderBrush="LightGray" BorderThickness="1" 
                   Padding="15" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Navigation Info:" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding CurrentSegment}" FontSize="11" Margin="5,0,0,3"/>
                    <TextBlock Text="Full Path:" FontWeight="Bold" FontSize="12" Margin="0,8,0,5"/>
                    <TextBlock Text="{Binding FullPath}" FontSize="11" Margin="5,0,0,3" FontFamily="Consolas"/>
                    <TextBlock Text="Parameter:" FontWeight="Bold" FontSize="12" Margin="0,8,0,5"/>
                    <TextBlock Text="{Binding ParameterInfo}" FontSize="11" Margin="5,0,0,0" FontFamily="Consolas"/>
                </StackPanel>
            </Border>
            
            <TextBlock Text="🎯 成功完成多層導覽！" 
                      FontSize="12" Foreground="Gray" 
                      HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
