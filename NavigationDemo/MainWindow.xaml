<Window x:Class="NavigationDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NavigationDemo.Views"
        xmlns:nav="clr-namespace:NavigationLib.FrameworksAndDrivers;assembly=NavigationLib"
        Title="NavigationLib Demo Application" 
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Control Panel -->
        <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="LightGray" BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <local:ControlPanelView Margin="10"/>
            </ScrollViewer>
        </Border>

        <!-- Right Panel: Main Content (ShellView) -->
        <Border Grid.Column="1" Background="White" Padding="10">
            <local:ShellView nav:Region.Name="Shell" />
        </Border>
    </Grid>
</Window>
